function getFullname(firstName, middleName, lastName, age) {
  const fullName = `Nama Saya : ${firstName} ${middleName} ${lastName} dan umur ${age} Tahun`;
  return fullName;
}

console.log(getFullname("Gerry", "Ajie", "Pratama", 35));
